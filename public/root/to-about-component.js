angular.module('toAbout', []);

angular.
    module('toAbout').
    component('toAbout', {
        templateUrl: 'public/root/to-about-template.html',
        controller: ['$scope', '$http',
            function toHomeController($scope, $http) {

            }
        ]
    });
